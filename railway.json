{
  "builder": "nixpacks",
  "nixpacks": {
    "providers": ["nodejs"],
    "build": {
      "commands": ["npm install", "npm run build"]
    },
    "start": {
      "cmd": "npm run start"
    }
  }
}
